import { useTheme } from '@mui/material'
import React from 'react'

const InspectorsData = () => {
    const theme = useTheme();
  return (
    <div>InspectorsData</div>
  )
}

export default InspectorsData